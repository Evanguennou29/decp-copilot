# Copy to .env.local for local development against a locally running API.
# VITE_API_BASE_URL=http://127.0.0.1:8000

# In production (Vercel), set VITE_API_BASE_URL to the deployed API's URL
# as a project environment variable — see the main README's deployment section.
